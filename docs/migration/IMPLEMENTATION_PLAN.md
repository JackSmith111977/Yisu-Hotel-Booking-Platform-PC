# 实现计划 (IMPLEMENTATION_PLAN)

**项目**: 酒店预订平台
**分支**: `feat/doc-first-system`
**最后更新**: 2026-03-28

---

## 1. 开发原则

### 1.1 文档优先
- 新功能开发前，先更新 PRD 和 APP_FLOW
- 接口变更先更新 BACKEND_STRUCTURE
- 组件新增先查看 FRONTEND_GUIDELINES

### 1.2 模块边界
- 不跨模块修改代码 (除非经过负责人同意)
- 公共组件放在 `src/components/global/`
- 工具函数放在 `src/lib/`

### 1.3 质量标准
- ESLint 零警告
- 关键业务逻辑有测试覆盖
- 复杂函数有 JSDoc 注释

---

## 2. 团队分工

### 2.1 Kei (Admin 模块 + 首页)
**职责**:
- Admin 后台所有页面的开发和维护
- 首页开发和优化
- 管理员服务 (`src/actions/admin_service.ts`)
- Admin 组件 (`src/components/admin/`)
- 首页组件 (`src/components/home/`)

**当前任务**:
- [ ] 仪表盘数据展示完善
- [ ] 审核流程优化
- [ ] 日志查询功能增强

---

### 2.2 成员 A (认证模块)
**职责**:
- 登录/注册页面的开发和维护
- 认证服务 (`src/actions/auth.ts`)
- 认证组件 (`src/components/auth/`)
- 邮件发送功能 (`src/lib/email.ts`)

**当前任务**:
- [ ] 登录流程优化
- [ ] 验证码发送频率限制
- [ ] 密码找回功能

---

### 2.3 成员 B (酒店商家模块)
**职责**:
- 商户后台所有页面的开发和维护
- 酒店服务 (`src/actions/hotels.ts`)
- 商户组件 (`src/components/hotel/`)
- 酒店数据 Hooks (`src/hooks/useHotels.ts`)

**当前任务**:
- [ ] 酒店管理表单完善
- [ ] 房型管理功能
- [ ] 图片上传优化

---

## 3. 开发流程

### 3.1 功能开发流程
```
1. 需求分析 (阅读 PRD.md)
   ↓
2. 确认模块归属 (查看 APP_FLOW.md)
   ↓
3. 创建功能分支 (git checkout -b feat/xxx)
   ↓
4. 开发实现 (遵循 FRONTEND_GUIDELINES.md)
   ↓
5. 自测 (npm run test)
   ↓
6. 代码检查 (npm run lint)
   ↓
7. 提交代码 (git commit -m "feat(xxx): description")
   ↓
8. 创建 PR
   ↓
9. 团队审查 → 合并
```

### 3.2 Bug 修复流程
```
1. 确认 Bug 现象
   ↓
2. 定位问题模块
   ↓
3. 创建修复分支 (git checkout -b fix/xxx)
   ↓
4. 修复实现
   ↓
5. 回归测试
   ↓
6. 提交代码 (git commit -m "fix(xxx): description")
   ↓
7. 创建 PR → 合并
```

---

## 4. AI 辅助开发

### 4.1 使用场景
| 场景 | 推荐操作 |
|------|----------|
| 新功能开发 | 先询问 AI 查看相关文档，再生成代码框架 |
| Bug 排查 | 提供错误信息，让 AI 分析可能原因 |
| 代码重构 | 使用 `/simplify` skill 优化代码 |
| 需求变更 | 使用 `/requirement-change` skill 管理变更 |
| 文档更新 | 让 AI 根据代码更新相关文档 |

### 4.2 常用指令
```
# 查看项目文档
请帮我查看 CLAUDE.md 中的开发规范

# 生成代码框架
根据 PRD.md 中的审核管理需求，生成 AuditTable 组件的代码框架

# 代码优化
/simplify 优化这段代码的可读性

# Bug 排查
这个错误是什么意思？[粘贴错误信息]
```

---

## 5. 测试策略

### 5.1 测试层级
```
┌─────────────────────────────┐
│       E2E 测试 (待建设)      │
├─────────────────────────────┤
│     集成测试 (优先建设)      │
├─────────────────────────────┤
│       单元测试 (必须)        │
└─────────────────────────────┘
```

### 5.2 测试优先级
| 优先级 | 模块 | 测试类型 |
|--------|------|----------|
| 高 | Server Actions | 单元测试 |
| 高 | 核心业务逻辑 | 单元测试 |
| 中 | 工具函数 | 单元测试 |
| 中 | 公共组件 | 组件测试 |
| 低 | 页面组件 | 组件测试 |

### 5.3 测试命令
```bash
npm run test           # 运行所有测试
npm run test:coverage  # 生成覆盖率报告
```

---

## 6. 交付清单

### 6.1 每次提交前检查
- [ ] 代码通过 ESLint 检查
- [ ] 测试通过
- [ ] Git 提交信息符合 Conventional Commits
- [ ] 相关文档已更新

### 6.2 每次发布前检查
- [ ] 所有功能测试通过
- [ ] 性能测试通过
- [ ] 安全扫描通过
- [ ] 文档同步更新

---

## 7. 沟通机制

### 7.1 日常沟通
- 使用即时通讯工具同步进度
- 遇到问题及时在群内提问

### 7.2 代码审查
- PR 描述清晰说明变更内容
- 审查意见及时响应
- 审查通过后及时合并

### 7.3 文档维护
- 发现文档错误及时修正
- 新增功能同步更新文档
- 定期回顾文档完整性

---

*本文档由 AI 生成，供团队开发和 AI 助手参考*
